#include <stdio.h>

int main ()
{
    float a,b,c;
    printf ("Enter first length of triangle: ");
    scanf ("%f", a);

    printf ("Enter first length of triangle: ");
    scanf ("%f", b);

    printf ("Enter first length of triangle: ");
    scanf ("%f", c);




    return 0;
}
